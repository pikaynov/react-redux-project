import React from 'react';
import css from './styles.css';

export const Footer = () => {
    const a = null;
    return (
        <footer className={css.footer}>
            footer
        </footer>
    )
}
